{
  "compilerOptions": {
    "strict": true,
    "outDir": "dist", // where to put the JS files
    "target": "ES2015", // JS language level (as a build target)
    "moduleResolution": "Node" // Find cjs modules in node_modules
  },
  "include": ["src"] // which files to compile
}